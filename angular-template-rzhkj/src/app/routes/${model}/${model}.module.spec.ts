import { ${className}Module } from './${model}.module';

describe('${className}Module', () => {
  let ${model}Module: ${className}Module;

  beforeEach(() => {
    ${model}Module = new ${className}Module();
  });

  it('should create an instance', () => {
    expect(${model}Module).toBeTruthy();
  });
});
