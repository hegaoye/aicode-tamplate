package $package$.$model$.mq;

import $package$.$model$.entity.$className$;

/**
 * 消息生产 接口
 */
public interface $className$Sender {

    /**
     * 创建 $className$
     *
     * @param $classNameLower$ $notes$ 的实体类
     */
    void build($className$ $classNameLower$);

}
